{
  "name": "action-create-emulator",
  "private": true,
  "main": "index.js",
  "author": "Igor Randjelovic <rigor789@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "esbuild --bundle --format=cjs --platform=node --outdir=dist ./index.js"
  },
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.14.51"
  }
}
