name: 'Create Emulator'
description: 'Create android emulator'
inputs:
  package:
    description: 'avd --package options'
    default: 'system-images;android-31;default;x86'
    required: false
runs:
  using: 'node16'
  main: 'dist/index.js'
branding:
  icon: 'smartphone'
  color: 'green'
